# CompliGuard CRE Secrets Declaration
# No secrets required — workflow reads public on-chain data via EVMClient
# and posts results via standard HTTP (no confidential keys needed).
#
# If you later add confidential API calls, declare secret names here:
# secretsNames:
#   MY_SECRET:
#     - MY_SECRET_ALL
